# 2026-08-21 (session 2) — Master Doc committed + deep-dive review fixes

## Task 1: Commit the Master Doc
- Committed `MASTER_DOC.md` + AGENTS.md Master-Doc hook + this memory file
  (commit 85a1fe3). 2,573 insertions.

## Task 2: AI chatbot / deep-dive memo — 12-point review fix
User attached the full NVDA institutional memo + a detailed review grading it
~7/10 architecture, ~5/10 analytical reliability, with 12 issues and a
"QUANTITATIVE INTEGRITY & ANTI-HALLUCINATION FIREWALL" prompt block. Fixed
every issue in `financial_llm_engine.py` + new `deep_dive_charts.py`:

1. **4-chart problem** → new `deep_dive_charts.py`: exactly 4 DISTINCT
   analytical charts per deep-dive (expectation_gap, scenario_distribution,
   valuation_sensitivity, risk_reward), each with chart_id/purpose/dataset/
   data_timestamp/provenance/calculation/run_id/dataset_hash. Process-wide
   `_CHART_HASH_REGISTRY` flags reused datasets (spec regenerated, never a
   cached figure). Wired into `ai_chatbot.process_enhanced_query` via
   `_is_deep_dive_query`; other queries keep price charts. DATA UNAVAILABLE
   state instead of fabricating.
2. **Scenario math** → `_dd_scenario_engine` + `_dd_round_scenarios` round
   probabilities (largest-remainder, sum = exactly 100%) and returns (whole
   percent) to DISPLAY precision BEFORE computing weighted return/drawdown
   probs — every displayed number recomputes exactly from the printed table.
   New QC3b (≤0.05pp tolerance). The reviewer's own recompute (-4.34%) now
   matches the displayed number.
3. **Market-vs-model disagreement** → §8/§19 compare market-implied REVENUE
   CAGR vs model REVENUE growth (same variable); FCF growth never compared
   against revenue growth. QC16.
4. **DCF labeling** → DCF prints exact formulas: D&A 6%×Rev, CAPEX 8%×Rev,
   **ΔNWC = 5%×ΔRevenue (CHANGE in revenue, not total)**. Status now
   CONDITIONAL/ASSUMPTION-BASED (was "PARTIAL").
5. **QC8-fail-but-DCF-authoritative** → §19 splits valuation into (A) reported,
   (B) assumption-based DCF (labeled CONDITIONAL/ILLUSTRATIVE), (C)
   market-implied, (D) scenario. QC21 verifies status.
6. **Reasons-to-own contradiction** → directional validation: only positive
   evidence under reasons-to-own, only negative under reasons-not-to-own
   (negative expected return can never appear under "reasons to own"). QC18.
7. **Rating** → `_dd_rating`: mechanically derived from expected-return band,
   risk-adjusted (P(>30% dd) ≥20% or confidence <50% → −1 notch), framework
   text printed.
8. **Confidence** → fixed disclosed weights DQ×30% + MR×25% + FC×25% + RC×20%,
   formula printed; QC17 recomputes independently.
9. **Bayesian** → labeled SUBJECTIVE BAYESIAN FRAMEWORK / Bayesian-Style
   Scenario Update (LRs are analyst-set, not calibrated).
10. **Fake precision** → every drawdown/loss prob is "modeled P(...) within
    defined scenarios" with empirical prob = DATA UNAVAILABLE (§13/14/19).
11. **Macro gating** → `_build_macro_analysis(..., current_data_available=False)`
    suppresses "The Fed remains…"/"here's what matters right now" claims;
    only transmission framework + DATA UNAVAILABLE note. QC19.
12. **LLM prompt** → 13-point QUANTITATIVE INTEGRITY FIREWALL added to the
    system prompt in `generate_analysis_llm`.

## Tests
- New `tests/test_deep_dive_review_fixes.py` (24 tests) covering the chart
  engine (4 distinct jobs, metadata, no-reuse hash, DATA UNAVAILABLE) and all
  integrity fixes.
- Updated `tests/test_deep_dive_integrity.py` (48 → 53) for the new wording
  (CONDITIONAL, "within defined scenarios", ΔRevenue formula).
- Full suite: **719 passed** (was 695).

## Master Doc
- Updated: deep-dive memo section (new helpers `_dd_scenario_engine`,
  `_dd_rating`, review-fix bullets), new Deep-Dive Chart Engine section + TOC
  entry, Test Suite table (695 → 719 + new file row), Changelog row.

## Task 3 (same day): Deep-dive retest review (round 2) fixes
User attached a second review grading the retest ~8.3/10 with 4 issues:

1. **Charts give DCF too much authority** → expectation-gap + valuation-
   sensitivity charts now print a prominent "CONDITIONAL DCF - NOT VERIFIED
   FCF VALUATION" banner (via `_conditional_banner_annotation`) and label bars
   "Cond. DCF Bear/Base/Bull" instead of bare "DCF Bear". CAVEAT FOUND:
   plotly `update_layout(annotations=[...])` MERGES by index instead of
   replacing, so the banner silently vanished from chart 1 — fixed by
   assembling `fig.layout.annotations` explicitly.
2. **Risk/reward chart semantically confusing** → rebuilt as a metrics TABLE
   matching the memo text: probability-weighted return, probability-weighted
   price, current price, upside/downside scenario ranges, weighted
   upside/downside contributions, modeled P(>30%/>50% drawdown) each "within
   defined scenarios (empirical: DATA UNAVAILABLE)".
3. **"0%" fake precision** → memo text was already compliant (modeled
   P(...) within defined scenarios); ALSO updated the eval prompts
   (`chatbot_eval/prompt_factory.py` `_DEEP_DIVE_TEMPLATES`, `llm_stress_test.py`)
   to request "MODELED probability ... within defined scenarios" instead of
   bare "probability of a >30% drawdown".
4. **Reasons-to-own semantic bug + hard QC** → extracted `_dd_build_reasons`
   (below-market DCF anchor is now a reason NOT to own — it was listed under
   reasons-to-own!) + `_dd_semantic_violations` (scans both lists for
   misplaced direction). QC18 is no longer hardcoded `True` — the audit runs
   the real scan and prints a SEMANTIC QC FAILURES block + FAIL on violation.

Also: every chart now carries `qc_status` from `_chart_qc` (visualization
inherits the memo's reconciliation rules — the reviewer's closing ask).

## Tests (round 2)
- Added 11 tests to `tests/test_deep_dive_review_fixes.py` (24 → 35):
  below-market DCF in reasons-not, semantic scan detection, QC18 real
  validation (planted violation → FAIL), conditional banners on charts 1 & 3,
  risk/reward table rows + reconciliation, `qc_status` on all charts,
  `_chart_qc` fail path, eval-prompt wording.
- Full suite: **730 passed** (was 719). First full run had 3 flaky
  paper-trading failures in test_integration_flows.py that pass in isolation
  and on clean rerun — load-related flakiness, not a code regression.

## Master Doc (round 2)
- Updated deep-dive chart engine section (banner, risk/reward table,
  qc_status), directional-validation bullet (real scan, below-market anchor),
  test counts 719 → 730, new Changelog row.

## Notes
- Chart no-reuse registry is process-wide; tests clear `_CHART_HASH_REGISTRY`.
- `deep_dive_charts.py` is a NEW module — future changes to the memo's
  scenario/DCF math must keep it in sync (it reuses `_dd_scenario_engine` and
  `_dd_real_dcf` from financial_llm_engine so numbers always match).
- Plotly gotcha: `update_layout(annotations=[...])` merges by index — build
  the full annotation list explicitly when a figure already has annotations.

# 2026-08-13

## Session: Dark Pool dashboard — continued

User said "continue" with no prior context; asked and they picked the Dark Pool dashboard.

### Found & fixed
The Dark Pool feature stored alerts (5 kinds) but **never evaluated them** — the UI
even claimed "Alerts evaluate on each scan". Implemented real evaluation:

- `dark_pool_engine.py`:
  - `add_alert` now assigns a `uuid` id.
  - `_alert_key()` — uuid for new alerts, content fingerprint (`legacy:...`) for
    pre-existing state files so old sessions still evaluate + honour cooldowns.
  - `_evaluate_alert(alert, report)` — per-kind conditions with documented
    0..1 threshold semantics:
    - `offex_pctile_90` -> 5d off-ex volume pctile >= threshold*100
    - `large_print` -> modeled block vol >= threshold * 20d avg volume
    - `imbalance_threshold` -> |5d imbalance| >= threshold
    - `activity_acceleration` -> |imbalance accel| >= threshold
    - `ai_unusual` -> top signal strength >= threshold*100
  - `evaluate_alerts(reports=None)` — one `analyze_ticker(include_live=False)` per
    symbol, persisted `alert_fired` cooldown log (6h, class attr
    `ALERT_COOLDOWN_HOURS`), pruned to 7 days, errors reported not raised.
- `dark_pool_ui.py` Alerts tab: "Check alerts now" button + spinner, per-kind
  threshold help on the selectbox, honest captions (cooldown + "never places trades").

### Tests
9 new offline tests in `tests/test_dark_pool_engine.py` (all kinds fire/stay silent,
cooldown + expiry, disabled/unknown kinds, unresolvable symbols, one-analyze-per-symbol,
legacy alert compatibility). 45/45 dark pool tests pass.

### Environment notes
- Use `.venv/bin/python` (system `python` not on PATH).
- Full suite has 5 pre-existing failures from missing deps in the venv:
  `pptx` (pitchbook tests) and `bcrypt` (Notification Settings tab) — unrelated to
  dark pool work.

### Repo state
Working tree clean before session; changes not yet committed.

## Post-lunch: zero-error sweep + commit (e9391c6)
- User asked: 0 errors anywhere (any feature/setup, no matter how niche), then commit.
- Fixed: missing requirements.txt (created from exact venv freeze; Dockerfile referenced it but it never existed), Dockerfile base 3.9->3.14 (3.9 can't install numpy 2.4/pandas 2.3/torch 2.9), stale hardcoded /Users/.../market_ai paths (repo moved to octavian) in main.py logo, data_downloader cache, 2 test sys.paths, START_HERE.md; 33 stale venv shebangs fixed locally; installed python-pptx + bcrypt into venv.
- REAL BUG found via flaky test: background_tasks._cleanup_locked stamped every record "running" at submit, so a flood of queued tasks could trip the flood fallback and evict the OLDEST record = the genuinely executing task, silently dropping long-running jobs (Breaking Trades/briefing toasts). Fix: new _EXECUTING set (worker adds itself at start, discards in finally); cleanup evicts finished/queued first, never executing. Added deterministic regression test; de-flaked eviction test (infinite gate wait + try/finally release instead of gate.wait(timeout=10), which expired under full-suite load).
- Lesson: when a timing test flakes, distrust the "timeout" — find the real race. The 10s gate timeout was masking an engine bug.
- Final: 552 passed / 0 failed; app boots headless HTTP 200. Committed e9391c6 (13 files, +667/-53).

## Dark Pool historical tab crash (user report) - fixed
- User reported: "NVDA: Insufficient history (need >= 30 trading days)" warning + TypeError 'float' object is not subscriptable at dark_pool_ui.py:956.
- Crash root cause: typo in _tab_historical - `labels = [k for k, _ in items if _[1] is not None]` subscripted the VALUE (a float) instead of checking it. Crashed on EVERY successful analysis (any ticker with data). The "Insufficient history" warning was a separate transient provider hiccup - NVDA fetch verified working (501 rows).
- Why tests missed it: test_ui_walkthrough NAV never included "Dark Pool Intelligence". Fixed by adding it to NAV + patching dark_pool_engine's module-level get_stock/get_vix/get_realtime_price bindings (data_sources patches don't reach it) + forcing no-key FINRA path. Verified the new walkthrough test reproduces the exact TypeError when the fix is reverted.
- Suite: 553 passed / 0 failed.

## Dark Pool historical tab crash (user report) - fixed
- User reported: "NVDA: Insufficient history (need >= 30 trading days)" warning + TypeError 'float' object is not subscriptable at dark_pool_ui.py:956.
- Crash root cause: typo in _tab_historical - the percentile-bar label comprehension sub-indexed the VALUE (a float) instead of checking it. Crashed on EVERY successful analysis (any ticker with data). The "Insufficient history" warning was a separate transient provider hiccup - NVDA fetch verified working (501 rows).
- Why tests missed it: test_ui_walkthrough NAV never included "Dark Pool Intelligence". Fixed by adding it to NAV + patching dark_pool_engine's module-level get_stock/get_vix/get_realtime_price bindings (data_sources patches don't reach it) + forcing no-key FINRA path. Verified the new walkthrough test reproduces the exact TypeError when the fix is reverted.
- Suite: 553 passed / 0 failed.

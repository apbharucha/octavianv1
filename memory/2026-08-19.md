# 2026-08-19 (session 3) — LLM 6.3/10 review fixes + algorithm builder math/ensemble fixes

## Task
User gave an 8-point review of the deep-dive memo (scored 6.3/10 as a financial LLM:
DCF untrustworthy, reverse-DCF single-rate error, expected-return contradiction,
wrong drawdown probs, smooth-scenario margins, unstamped price, boilerplate macro,
"Bayesian" without evidentiary strength, QC audit that checks format not finance)
plus four algorithm-builder/portal bugs: per-window Sharpe always > overall Sharpe;
trades table missing symbol/asset type; ENSEMBLE card showing return/Sharpe with
0 trades; quick-select asset buttons should pick most relevant assets. "Use the
tools already within the site" (DCF tool) and iterate fix→test→fix→test.

## What I did

### Algorithm builder (`algorithm_builder_engine.py`, `algorithm_builder_ui.py`)
- `classify_asset_type()` (futures =F / FX =X / crypto -USD / index ^ / ETF list / equity)
  + `_stamp_trades()`: every trade now carries symbol + asset_type; trade table,
  narratives and markdown export show them.
- ENSEMBLE: aggregated constituent trades (source + blend weight) into compute_metrics
  and window_metrics — fixes the Return 79.74% / TRADES 0 / WIN RATE 0.00 contradiction.
- Sharpe: verified the mean/std·√252 (ddof=1) formula is identical overall vs per-window;
  fixed an off-by-one in window slicing (`-1 - bars:` gave bars+1 obs but reported bars);
  added `bars` column + caption (short windows are small samples; overall includes the
  pre-first-trade flat stretch → window Sharpe legitimately can exceed overall).
- Add/cash: trade table highlights include symbol; ensemble caption explains union semantics.

### Quant portal (`quant_portal.py`)
- Quick-select Stocks/Futures/FX/Crypto now fill curated mega-liquid names first
  (AAPL/MSFT/NVDA/...; ES=F/NQ=F/CL=F/...; EURUSD=X/...; BTC-USD/...), padded from the
  live universe, with fallback lists when the universe fetch fails.

### Deep-dive memo (`financial_llm_engine.py`) — the 6.3/10 review
- DCF gating: BLOCKED when no REPORTED income statement; PARTIAL with explicit disclosure
  that FCF = NOPAT − ASSUMED reinvestment because the cash-flow statement is DATA
  UNAVAILABLE. Mechanics now run through the SITE's own `InstitutionalDCFEngine`
  (compute_wacc CAPM cross-check + project_fcf 20-line table, transported back).
- Reverse DCF: proper multi-variable solve (implied revenue CAGR / EBIT margin / FCF
  growth via deterministic bisection on the same DCF equation) + explicit statement that
  implied FCF growth ≠ implied revenue growth; coarse Gordon grid kept as secondary.
- Single-source exp_ret and P(>30%/>50% drawdown) = Σ p where r ≤ level (fudge factor
  `EB + 0.5·Bear` removed); the committee's "positive expected return" hardcode is now
  the actual signed number.
- Scenario margins move with growth via operating leverage (0.2pp/pp, clipped ±6pp).
- Price is stamped: quote_date + source (yahoo 5d daily, delayed EOD) in header AND §19.
- Macro transmission: every range labeled SCENARIO ASSUMPTION; +100bps-WACC fair-value
  impact from the §6 DCF is the one MODEL CALCULATION.
- Bayesian: Evidence-basis column + honest calibration caveat (LRs are analyst-set
  MODEL ASSUMPTIONS, a decision framework, not statistics).
- QC audit: 15 checks incl. numerical reconciliation (weighted return, weighted price,
  P(dd>30/50) recomputed, DCF independently recomputed to 1e-6, cash-flow-verified);
  any failure → "AUDIT PARTIAL PASS — N QUANTITATIVE CHECK(S) FAILED" + DCF STATUS note.
  No more blanket AUDIT PASS when cash-flow data is missing (QC8 deliberately FAILS).

### Tests
- `tests/test_deep_dive_integrity.py`: 53 (10 new: DCF blocked/partial, multi-var reverse,
  exp-return/drawdown consistency across §13/14/19, margin monotonicity, price stamp,
  macro labels + WACC shock, Bayesian basis, QC8-fails audit).
- `tests/test_algorithm_builder.py`: 74 (asset-type map, stamped trades + markdown,
  ensemble aggregation/win-rate, Sharpe formula identical + recomputes, bars==slice_len).
- `tests/test_ui_walkthrough.py`: quick-select asserts curated mega-caps (AAPL first,
  BTC-USD first for crypto). Full suite: 694 passed (3:42), boot HTTP 200, pyflakes clean.

## Gotchas
- pandas `.to_markdown()` needs optional `tabulate` (not installed) → wrote `_df_to_markdown`.
- The site's `InstitutionalDCFEngine._calculate_reverse_dcf` scipy fsolve is broken here
  (prints "Quick DCF error: only 0-dimensional arrays..." and silently falls back to
  defaults) → used its project_fcf / compute_wacc (deterministic, working) + own bisection
  solve for the multi-variable reverse DCF.
- `re.match` needs the string arg. `out.splitlines()[1]` is a blank line, not the price line.
- Streamlit `use_container_width` is deprecated (warning only).
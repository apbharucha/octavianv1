[pytest]
asyncio_mode = auto
# test_before_start.py is a standalone pre-flight check (module-level sys.exit)
# that is run manually via `python tests/test_before_start.py` — not a pytest test.
addopts = --ignore=tests/test_before_start.py
